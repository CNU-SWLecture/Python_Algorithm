a = [5 ,1, 8, 1, 2]
capacity = 5
length = 5

new_data = 5

if length < capacity:
    a[length] = new_data
    length += 1
elif length == capacity:
    capacity = capacity * 2
    b = [None, for x in range(capacity)]
for idx in range(length):
    b[idx] = a[idx]
    print(a)
    print(id[a])
    
for idx in range(length:
    )