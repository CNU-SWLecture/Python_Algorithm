def list_append(a, length, capacity, data):
    if length == capacity:
        capacity = capacity * 2
        b = [None for x in range(capacity)]
        for idx 
a = [2, 1, 4, 5]
a_length = 4
a_capacity = 4